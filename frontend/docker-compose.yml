services:
  postgres:
    image: postgres:16
    container_name: aztec-bridge-db
    environment:
      POSTGRES_USER: aztec
      POSTGRES_PASSWORD: aztec
      POSTGRES_DB: aztec_bridge
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
